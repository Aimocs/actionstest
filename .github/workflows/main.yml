name: Test

on:
  push:

jobs:
  tests:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: ./index.js 
      - uses: actions/setup-node@v2
        with:
          node-version: 16
      - uses: ./
